@using NextERP.ModelBase
@using NextERP.Util
@model NextERP.ModelBase.SupplierModel
@inject LazZiya.ExpressLocalization.ISharedCultureLocalizer Localizer

<div class="modal fade" id="create-edit-modal" aria-hidden="true" tabindex="-1">
    <div class="modal-dialog modal-dialog-centered modal-dialog-scrollable">
        <div class="modal-content">
            <form class="needs-validation" id="@TableName.Supplier" novalidate>
                <div class="modal-header">
                    <h1 class="modal-title fs-5" id="exampleModalLabel" localize-content>CreateOrEdit</h1>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <input id="@SupplierModel.AttributeNames.SupplierId" type="hidden" asp-for="Id" />

                    <div class="mb-2">
                        <label for="@SupplierModel.AttributeNames.SupplierCode" class="col-form-label col-md-6" localize-content>@SupplierModel.AttributeNames.SupplierCode</label>
                        <div class="col-md-12">
                            <input class="form-control form-control-sm" type="text" asp-for="SupplierCode" disabled />
                        </div>
                    </div>

                    <div class="mb-2">
                        <label for="@SupplierModel.AttributeNames.SupplierName" class="col-form-label col-md-6" localize-content>@SupplierModel.AttributeNames.SupplierName</label>
                        <div class="col-md-12">
                            <input class="form-control form-control-sm" type="text" asp-for="SupplierName" />
                            <div class="invalid-feedback" id="invalid-@SupplierModel.AttributeNames.SupplierName" localize-content></div>
                        </div>
                    </div>

                    <div class="mb-2">
                        <label for="@SupplierModel.AttributeNames.ContactName" class="col-form-label col-md-6" localize-content>@SupplierModel.AttributeNames.ContactName</label>
                        <div class="col-md-12">
                            <input class="form-control form-control-sm" type="text" asp-for="ContactName" />
                            <div class="invalid-feedback" id="invalid-@SupplierModel.AttributeNames.ContactName" localize-content></div>
                        </div>
                    </div>

                    <div class="mb-2">
                        <label for="@SupplierModel.AttributeNames.PhoneNumber" class="col-form-label col-md-6" localize-content>@SupplierModel.AttributeNames.PhoneNumber</label>
                        <div class="col-md-12">
                            <input class="form-control form-control-sm" type="text" asp-for="PhoneNumber" />
                            <div class="invalid-feedback" id="invalid-@SupplierModel.AttributeNames.PhoneNumber" localize-content></div>
                        </div>
                    </div>

                    <div class="mb-2">
                        <label for="@SupplierModel.AttributeNames.Address" class="col-form-label col-md-6" localize-content>@SupplierModel.AttributeNames.Address</label>
                        <div class="col-md-12">
                            <textarea class="form-control form-control-sm" rows="3" asp-for="Address"></textarea>
                            <div class="invalid-feedback" id="invalid-@SupplierModel.AttributeNames.Address" localize-content></div>
                        </div>
                    </div>

                    <div class="mb-2">
                        <label for="@SupplierModel.AttributeNames.Email" class="col-form-label col-md-6" localize-content>@SupplierModel.AttributeNames.Email</label>
                        <div class="col-md-12">
                            <input class="form-control form-control-sm" type="email" asp-for="Email" />
                            <div class="invalid-feedback" id="invalid-@SupplierModel.AttributeNames.Email" localize-content></div>
                        </div>
                    </div>


                    <div class="mb-2">
                        <label for="@SupplierModel.AttributeNames.Note" class="col-form-label col-md-6" localize-content>@SupplierModel.AttributeNames.Note</label>
                        <div class="col-md-12">
                            <textarea class="form-control form-control-sm" rows="3" asp-for="Note"></textarea>
                            <div class="invalid-feedback" id="invalid-@SupplierModel.AttributeNames.Note" localize-content></div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal" localize-content>Close</button>
                    <button type="button" class="btn btn-outline-primary save-button" id="only-save-button" localize-content>Save</button>
                    <button type="button" class="btn btn-primary save-button" data-bs-dismiss="modal" localize-content>Save&Close</button>
                </div>
            </form>
        </div>
    </div>
</div>

<script>
    $(".save-button").click(function () {
        const $form = $(this).closest("form");
        const formData = new FormData($form[0]);
        const formElement = $form[0];
        let id = formData.get("@Constants.Id");

        // Nếu không có ID (tạo mới), set Guid.Empty
        if (!id || id === "") {
            formData.set("@Constants.Id", "@Guid.Empty");
        }

        callApi("@Url.Action(Constants.CreateOrEdit, TableName.Supplier)", "POST", formData)
            .then(data => {
                loadSuppliers();

                if (data && Array.isArray(data)) {
                    showInvalid(data, formElement);
                }
            })
            .catch(err => {
                showMessage("@Localizer[Messages.ErrorCallAPI]");
            });
    });
</script>
