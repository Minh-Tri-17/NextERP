@using NextERP.ModelBase
@using NextERP.Util
@model NextERP.ModelBase.NotificationModel

@{
    ViewData["Title"] = TableName.Notification;
    Layout = "~/Views/Shared/_Layout.cshtml";
    ViewBag.Breadcrumbs = new List<(string Title, string Url, bool IsActive)>
    {
        (TableName.Notification + "List", "", true),
    };
}